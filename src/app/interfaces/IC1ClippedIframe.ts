export interface IC1ClippedIframe {
    insertGlobalcss(css: string): void;
    insertCodeBlock(code: string): void;
    // insertbg(img1: string, img2: string): void;
    insertLogo(logo: string): void;
}